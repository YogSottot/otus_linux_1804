# После применения Vagrant-файла, получаем готовую к загрузке систему с LVM и ext4.
# Отключаем старый диск в virtualbox и загружаемся в новую систему.


lvcreate -L 8g -s -n home-snap /dev/VolGroup01/home && \
